language: c

cache: build

jobs:
  include:
    - stage: pre-test
      script:
      - rm -rf build ; mkdir build
      - touch build/pre-test1.ran
      - ls -lRa
      script:
      - rm -rf build ; mkdir build
      - touch build/pre-test2.ran
      - ls -lRa
    - stage: test
      script:
      - touch test.ran
      - ls -lRa
